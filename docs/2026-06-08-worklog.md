# 2026-06-08 Worklog

## What Changed

- Migrated the full Dink plugin implementation from the nested `Existing Plug` directory into the repository root.
- Replaced the example-template source tree with the real `TcCrewPlugin` source, tests, resources, docs, metadata, and assets.
- Removed the obsolete nested plugin folder after the migration.
- Switched the root Gradle scripts from Kotlin DSL to Groovy DSL so the project can be evaluated on this machine without the Kotlin script parser failing on local Java `25.0.1`.
- Added a remote clan-event polling and activation flow driven by a static/configurable remote JSON endpoint.
- Added a `::DinkEvent event enable <eventId>` command that validates the active remote event, downloads an allowlisted migration payload, and applies clan-event settings locally.
- Added persisted remote-event state for last seen config version, last prompted event ID, and applied event IDs to prevent prompt spam and duplicate activation.
- Added a root Gradle `run` task and restored a checked-in IntelliJ `.run/Run Dink.run.xml` launcher configuration for `tccrewplugin.DinkTest`.

## Current State

- The repository root now contains the actual Dink plugin project rather than the RuneLite example plugin template.
- The plugin metadata points at `tccrewplugin.TcCrewPlugin`.
- Documentation from the migrated project is available in `README.md` and `docs/`.
- Remote event polling is wired into plugin startup, config changes, and shutdown using the existing asynchronous OkHttp and scheduled-executor patterns.
- The repo now has both `gradlew run` and an IntelliJ Application config path for launching the development client.

## Verification Notes

- Confirmed the `Existing Plug` directory no longer exists.
- A `gradlew test` run initially failed before project evaluation because the Kotlin DSL parser rejected local Java version `25.0.1`.
- Root build scripts were converted to Groovy DSL to remove that blocker.
- Per the attached implementation request, I did not continue chasing test/build execution because the local Java toolchain is still on Java `25.0.1`, which is outside the expected RuneLite build path for this repo.

## Next Steps

- Point Gradle/RuneLite at a compatible JDK, then run `./gradlew run`.
- Test remote polling with a hosted config payload and then verify `::DinkEvent event enable <eventId>` applies the expected clan-event settings once.
- Continue with the next clan-event or notification enhancements after in-game verification.

